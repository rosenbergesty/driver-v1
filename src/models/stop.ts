/**
 * Stop Model
 */
export class Stop {
  // constructor(fields: any) {
  //   // Quick and dirty extend/assign fields to this model
  //   for (const f in fields) {
  //     this[f] = fields[f];
  //   }
  // }


  ID: number;
  address: string;
  type: string;

}